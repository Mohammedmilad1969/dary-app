rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow everything - fully open for testing
    // This will work regardless of Firebase Auth state
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
